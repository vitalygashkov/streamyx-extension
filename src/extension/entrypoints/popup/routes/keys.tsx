import { Layout } from '../components/layout';
import { Header } from '../components/header';

export const Keys = () => {
  return (
    <Layout>
      <Header backHref="/">Keys</Header>
    </Layout>
  );
};
